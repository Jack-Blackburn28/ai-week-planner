# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (never commit secrets)
.env
.env.*
!.env.example

# Google integration: encrypted tokens + calendar mapping (never commit)
.tokens.json
.google-config.json

# Granola integration + todo state: encrypted token + persisted stores (never commit)
.granola-tokens.json
.granola-store.json
.completions.json

# typescript
*.tsbuildinfo
next-env.d.ts

# editor
.vscode/*
!.vscode/extensions.json
.idea
