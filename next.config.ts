import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Strict mode surfaces potential problems early — good default for a fresh app.
  reactStrictMode: true,
};

export default nextConfig;
